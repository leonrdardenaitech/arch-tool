# ATLAS CORE: Director's Handshake Guide

To move from **Dormant Standby** to **Active Production**, the following handshakes must be finalized:

## 1. The Knowledge Brain (NLM)
- **Command:** `nlm login` (in a separate terminal).
- **Result:** Activates the `nlm_ingestion_engine.py` to auto-sync your web clippings.

## 2. The Video Assembly Line (FFmpeg)
- **Action:** Add your FFmpeg /bin folder to Windows System PATH.
- **Result:** Activates the `lofi_sequence_watcher.py` to auto-stitch your `00001-00006` downloads.

## 3. The B2B Bridge (Zapier)
- **Script:** Run `python public/LeonRDarden_Hub_Staging/Atlas_MCP_Server/Phase2_Zapier_Link.py`.
- **Result:** Unlocks 8,000+ real-world app actions for your AI agent.

## 4. The Action Arm (Gemma 4)
- **Action:** Side-load the `/dist` folder in `Gemma4_Extension_Vault` to Chrome.
- **Result:** Enables autonomous browser scraping and UI interaction.

*The Master Brain is locked, synchronized, and pre-produced.*
